# NotebookLM 逐页提示词：FoodFlow 11 页答辩 PPT

当总提示词生成效果不稳定时，使用本文件逐页生成或修订。全局要求：中文 16:9，科研答辩风，白色背景，深蓝主色，少量正式红强调结论；图表页优先使用上传的 PNG 图表；不编造数据；不把合成骑手说成真实派单数据。

## Slide 1：FoodFlow：外卖三方推荐与动态履约仿真系统

页面目标：建立项目主题和三方视角。

必须出现的文本：

- FoodFlow
- 外卖三方推荐与动态履约仿真系统
- 用户 × 商家 × 骑手
- 课程大作业答辩

视觉布局：

- 中心放“推荐系统 / FoodFlow”节点。
- 三个方向连接“用户”“商家”“骑手”。
- 底部用小字写“真实 TRD 订单数据 + 合成骑手履约仿真”。

演讲备注：

本项目关注外卖平台中的三方推荐问题。用户侧要推荐合适商家，商家侧要获得合理曝光，骑手侧要承接可履约的订单。FoodFlow 的主线是把推荐结果接入动态履约仿真，用推荐指标和系统级指标共同评价策略。

## Slide 2：为什么外卖推荐不能只看 Recall

页面目标：提出问题，说明普通推荐指标不够。

必须出现的文本：

- 推荐列表会改变订单空间分布
- 订单空间分布会影响 ETA、超时率和骑手负载
- 热门商家集中曝光会挤压长尾商家
- 所以外卖推荐需要用户、商家、骑手三方共同评价

视觉布局：

- 左到右流程：推荐 -> 下单 -> 派单 -> 配送 -> 反馈。
- 在“下单”和“派单”之间标注“空间分布变化”。
- 右侧放 3 个风险标签：履约风险、曝光集中、负载不均。

演讲备注：

普通推荐系统通常先看用户是否点击或下单，但外卖场景还有强履约属性。推荐如果把订单推向远距离或骑手供给不足的商家，即使命中了用户兴趣，也可能带来更高 ETA 和超时。因此我们把推荐和派单仿真连起来评价。

## Slide 3：数据来源与可复现边界

页面目标：满足课程硬性要求，交代数据来源和合成数据边界。

必须出现的文本：

- Takeout Recommendation Dataset (TRD)
- Zenodo DOI：10.5281/zenodo.8025855
- 美团外卖北京 11 个商圈
- 时间范围：2021-03-01 至 2021-03-28
- 使用 users、pois、spus、orders_train、orders_test、labels
- 骑手状态为固定 seed 合成 proxy，不声称为真实派单数据

视觉布局：

- 左侧放数据来源卡片。
- 右侧放数据表关系图：用户、商家、菜品、训练订单、测试订单、标签。
- 底部放“可复现边界”：跳过 1.8GB graph.bin；CPU 可运行。

演讲备注：

数据主线选择 TRD，因为它是公开 Zenodo 数据，包含用户、商家、菜品、订单和测试标签，适合做标准推荐评估。TRD 没有完整骑手状态，所以骑手部分使用固定 seed 合成，只作为履约约束的 proxy，并在报告中明确说明。

## Slide 4：系统总体架构

页面目标：说明实现过程和模块闭环。

必须出现的文本：

- 数据下载与清洗
- 用户/商家/菜品特征
- 多路推荐基线
- 三方重排
- 订单-骑手匹配
- 午餐高峰动态仿真
- 指标、图表、报告、Demo

视觉布局：

- 横向流水线架构图。
- 每个模块用矩形节点，节点之间用箭头连接。
- 在输出端列出 CSV、PNG、实验报告、Streamlit Demo。

演讲备注：

系统被拆成可重复执行的工程模块：先完成数据处理和特征构建，再训练多个推荐基线，之后通过三方重排加入商家公平和履约约束。最后把推荐列表转化为模拟订单，进入骑手匹配和动态仿真，输出指标、图表和报告。

## Slide 5：推荐算法与三方重排

页面目标：说明算法设计思路和创新点。

必须出现的文本：

- Baselines：Random、Popular、Repeat、ItemCF、BPR-MF
- UserOnly：品类偏好 + 复购 + 价格匹配 + 时段偏好 + 商家质量
- Seq-Hybrid：复购序列 + 最近订单衰减 + 商家转移概率
- Seq-xQuAD：序列相关性 + 品类覆盖 + 长尾曝光
- Seq-xQuAD-Tripartite：列表级覆盖 + 商家公平 + ETA + 供给分
- 可解释输出只引用真实参与打分的特征

视觉布局：

- 左侧放模型对照表。
- 右侧放 Seq-xQuAD-Tripartite 打分拆解：

```text
base_score = user_preference + fairness + eta_score + supply_score
list_score = relevance + category_coverage + long_tail_gain
```

- 用不同颜色标注用户、商家、骑手/履约相关项。

演讲备注：

基线覆盖随机、热门、复购、协同过滤和矩阵分解。Seq-Hybrid 把外卖复购和最近订单序列建进去；Seq-xQuAD 在此基础上做列表级覆盖；Seq-xQuAD-Tripartite 再加入商家公平、ETA 和供给分。这样推荐列表不只对用户排序，也把商家和履约约束纳入排序。

## Slide 6：离线推荐指标结果

页面目标：展示标准推荐指标与商家曝光指标。

必须使用图片：

- `offline_recall20.png`
- `tradeoff_ndcg_gini.png`

建议可补充：

- `offline_ndcg20.png`
- `offline_coverage20.png`
- `offline_exposure_gini.png`

必须出现的文本：

- Seq-xQuAD Recall@20 = 0.4447，NDCG@20 = 0.3538
- Seq-Hybrid Recall@20 = 0.4441，NDCG@20 = 0.3513
- UserOnly Recall@20 = 0.4287，NDCG@20 = 0.3423
- Repeat Recall@20 = 0.4062，体现外卖复购特征
- Seq-xQuAD-Tripartite Recall@20 = 0.4180，NDCG@20 = 0.3440
- Seq-xQuAD-Tripartite 不是追求单一 Recall 最大，而是在准确性、公平性和履约之间折中

视觉布局：

- 左侧放 `offline_recall20.png`。
- 右侧放 `tradeoff_ndcg_gini.png`。
- 底部放一句结论带：推荐有效，但只看 Recall 会漏掉商家曝光和履约风险。

演讲备注：

离线结果显示，Seq-xQuAD 的 Recall@20 和 NDCG@20 最高，说明外卖复购序列和列表级覆盖重排有效；Repeat 也很强，符合外卖复购规律。Seq-xQuAD-Tripartite 的离线准确率有所下降，但 NDCG@20 仍略高于 UserOnly，它的价值要结合三方履约指标继续判断。

## Slide 7：动态履约仿真设计

页面目标：说明订单如何进入骑手侧推荐/匹配。

必须出现的文本：

- 午餐高峰多时间步：每 5 分钟生成一批用户请求
- 推荐列表产生模拟订单：Top-K 商家进入用户选择模型
- 订单推荐给骑手：Nearest、MinETA、LoadAware
- 状态更新形成闭环：骑手位置、负载、完成时间影响下一轮

视觉布局：

- 中央画闭环：用户请求 -> 商家推荐 -> 用户下单 -> 订单-骑手匹配 -> 状态更新 -> 下一时间步。
- 右侧小指标面板：Avg ETA、Timeout Rate、Rider Load Std、Platform Utility。

演讲备注：

仿真把推荐从“列表”推进到“履约结果”。每个时间步产生用户请求，推荐器给出 Top-K 商家，选择模型生成订单；随后订单会被分配给候选骑手。骑手状态更新后影响下一轮，这样可以观察推荐策略对履约效率的连锁影响。

## Slide 8：三方策略的系统级指标

页面目标：展示动态仿真结果，证明三方策略带来系统级收益。

必须使用图片：

- `simulation_avg_eta.png`
- `simulation_platform_utility.png`

建议可补充：

- `simulation_timeout_rate.png`
- `simulation_rider_load_std.png`
- `simulation_exposure_gini.png`

必须出现的文本：

- UserOnly + Nearest：Avg ETA = 86.54，Timeout Rate = 0.8721，Utility = 0.3998
- UserOnly + MinETA：Avg ETA = 55.33，Timeout Rate = 0.6701，Utility = 0.4831
- Seq-Tripartite：Avg ETA = 52.15，Timeout Rate = 0.6364，Utility = 0.4963
- Ours-Full：Avg ETA = 50.85，Timeout Rate = 0.5714，Utility = 0.5246
- Seq-xQuAD-Tripartite：Avg ETA = 50.33，Timeout Rate = 0.5319，Utility = 0.5264
- Seq-xQuAD-Tripartite 平台综合效用最高

视觉布局：

- 上方放两个主图：平均 ETA、平台效用。
- 下方放小表格，列出三条关键策略的 ETA、超时率、效用。
- 用正式红突出 Seq-xQuAD-Tripartite 的 `0.5264`。

演讲备注：

仿真结果说明，单纯 UserOnly 加最近骑手会造成很高 ETA 和超时率。改为最小 ETA 派单后明显改善；继续加入列表级覆盖、履约感知和三方重排后，Seq-xQuAD-Tripartite 平均 ETA、超时率和平台效用最好，证明推荐策略需要接受履约结果检验。

## Slide 9：案例解释：一次推荐如何兼顾三方

页面目标：让老师直观看到可解释三方链路。

必须出现的文本：

- 用户画像：复购、品类偏好、价格区间、时段
- 商家推荐：品类匹配、价格匹配、商家质量、曝光补偿、ETA
- 下单后：订单匹配给候选骑手
- 骑手侧：ETA、当前负载、可靠性
- 推荐不是黑箱，而是可解释的三方决策

视觉布局：

- 三栏布局：
  - 左栏：用户画像卡片。
  - 中栏：Top-3 推荐商家列表，每个商家带推荐原因。
  - 右栏：骑手匹配卡片，展示候选骑手、ETA、负载、最终选择。
- 底部画一条链路：用户 -> 商家 -> 订单 -> 骑手。

演讲备注：

这一页用于解释三方推荐不是抽象概念。用户看到的是符合复购、品类和价格偏好的商家；商家侧通过曝光补偿避免全部流量集中在头部；用户下单后，订单再根据 ETA、负载和可靠性匹配给骑手，形成完整决策链路。

## Slide 10：结论、局限与答辩亮点

页面目标：收束贡献，主动说明局限。

必须出现的文本：

- 结论 1：标准推荐指标证明模型有效
- 结论 2：三方重排把商家公平和履约约束引入推荐
- 结论 3：Seq-xQuAD-Tripartite 以一部分准确率代价换取更低 ETA、更低超时率和最高平台效用
- 局限：骑手数据为合成 proxy；BPR-MF 为轻量实现；平台效用权重是解释性设置
- 后续：接入真实派单数据、敏感性分析、图模型扩展

视觉布局：

- 上方三个结论卡片。
- 下方一个“局限与后续”横条。
- 右侧放关键数字摘要：Seq-xQuAD Recall@20 0.4447、Seq-xQuAD-Tripartite Avg ETA 50.33、Utility 0.5264。

演讲备注：

项目完成了可运行的推荐与履约仿真闭环。离线指标证明推荐不是随机有效，Seq-xQuAD 把离线排序指标继续推高；Seq-xQuAD-Tripartite 让商家公平和履约可行性进入排序，系统级仿真显示它的平台效用最高。局限也很明确：骑手数据是 proxy，后续应接入真实派单并做更充分的敏感性分析。

## Slide 11：Q&A

页面目标：简洁结束，留下可复现印象。

必须出现的文本：

- 数据可复现
- 指标可对比
- 模块可运行
- Q&A

视觉布局：

- 居中大标题 Q&A。
- 下方三个关键词徽章：数据可复现、指标可对比、模块可运行。
- 背景可以用淡化的三方关系图，不要复杂。

演讲备注：

最后强调项目不是只做展示页面，而是有数据来源、可重复执行脚本、标准推荐指标、三方仿真指标和实验报告支撑。欢迎围绕数据边界、指标设计、三方重排和动态仿真提问。
